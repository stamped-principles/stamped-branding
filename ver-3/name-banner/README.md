# Banner vectorization instructions

For most icons, GIMP was used to create a transparent background (add alpha channel -> choose by color, adjust threshold, delete) which was then imported into Inkscape and a bitmap was traced using pixel art (Voronoi, max settings) and if transparent, duplicates were overlayed
